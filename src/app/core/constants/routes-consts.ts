export const LayoutRoutes = {
    MAIN: 'main',
} as const;

export const MainRoutes = {
    HOME: 'home',
    STARTEXAM: 'start-exam',
    SETTINGS: 'settings',
    QUESTIONBANK: "question-bank"
} as const;
